name: Build and Deploy to server

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.DEPLOYMENT_KEY }}

    - name: Set up environment variables
      run: |
          echo "API_URL= ${{ vars.DEV_API_URL }}" >> .env
          echo "ACCESS_AUTH_TOKEN= ${{ vars.DEV_AUTH_API_TOKEN }}" >> .env

    - name: Verify .env content
      run: cat .env

    - name: Build and deploy
      env:
        DEPLOYMENT_SERVER: ${{ secrets.DEPLOYMENT_SERVER }}
        DEPLOYMENT_USER: ${{ secrets.DEPLOYMENT_USER }}
      run: |
        ssh -o StrictHostKeyChecking=no $DEPLOYMENT_USER@$DEPLOYMENT_SERVER << 'EOF'
          cd htdocs/bloom/
          git pull origin develop
          npm install
          cd backend
          npm install
          pm2 restart all
        EOF